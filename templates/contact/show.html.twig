{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    {# <h1>Contact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact.id }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ contact.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact.email }}</td>
            </tr>
            <tr>
                <th>Linkedin</th>
                <td>{{ contact.linkedin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contact.index') }}">back to list</a>

    <a href="{{ path('contact.edit', {'id': contact.id}) }}">edit</a>

    {{ include('contact/_delete_form.html.twig') }} #}
<div class="card text-left">
    <div class="card-body">
        <div class="d-flex justify-content-between flex-wrap align-items-center mb-3">
            <h4 class="card-title mb-0">Détails de l'adresse</h4>
            <div class="d-flex justify-content-end w-100 px-4">
                <a href="{{ path('contact.index') }}" class="mr-3 text-info">Retour à la liste</a>
                |
                <a href="{{ path('contact.edit', {'id': contact.id}) }}" class="mx-3 text-success">Modifier l'utilisateur</a>
                |
                <div class="ml-3">
                    {{ include('contact/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
        <table class="table table-borderless">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ contact.id }}</td>
                </tr>
                <tr>
                    <th>Téléphone</th>
                    <td>{{ contact.phone }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ contact.email }}</td>
                </tr>
                <tr>
                    <th>LinkedIn</th>
                    <td>{{ contact.linkedin }}</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
