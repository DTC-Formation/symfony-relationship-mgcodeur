{% extends 'base.html.twig' %}

{% block title %}
    {{ user.firstName }} {{ user.lastName }}
{% endblock %}

{% block body %}
    <div class="card user-profile o-hidden mb-4">
        <div class="header-cover" style="background-image: url('{{ asset('static/images/backgrounds/cover-1.jpg') }}')"></div>
        <div class="user-info"><img class="profile-picture avatar-lg mb-2" src="{{ asset('static/images/avatars/me.jpg') }}" alt="">
            <p class="m-0 text-24">{{ user.firstName }} {{ user.lastName }}</p>
            <p class="text-muted m-0">{{ user.age }} ans</p>
            <div class="d-flex justify-content-end w-100 px-4">
                <a href="{{ path('user.index') }}" class="mr-3 text-info">Retour à la liste</a>
                |
                <a href="{{ path('user.edit', {'id': user.id}) }}" class="mx-3 text-success">Modifier l'utilisateur</a>
                |
                <div class="ml-3">
                    {{ include('user/form/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
        <div class="card-body">
            {# TODO: adresse, contact, etudes #}
        </div>
    </div>
{% endblock %}
